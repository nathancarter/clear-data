{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run unit tests",
            "type": "shell",
            "command": "python3 -m unittest -v tests/test_*",
            "problemMatcher": []
        },
        {
            "label": "Build source docs",
            "type": "shell",
            "command": "python3 -m pdoc --html src/clear_data --force",
            "problemMatcher": []
        }
    ]
}